name: openssl-LTS for windows

on:
  workflow_dispatch:
    
jobs:
  build:
    uses: ./.github/workflows/test.yml
    with:
      major: 1
      minor: 1
      patch: 1w
      REPO_BRANCH: OpenSSL_1_1_1w